app = "telegram-bot-yzrlsw"

[build]
  builder = "paketobuildpacks/builder:base"

[[services]]
  internal_port = 8080
  protocol = "tcp"

  [[services.ports]]
    handlers = []
    port = 8080

[env]
  # Сюда ничего не пиши. Секреты задал через UI Fly.io — этого достаточно.
